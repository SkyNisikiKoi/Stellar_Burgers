import { dataCards } from './data.js';
import { api } from './Api.js';

// export let bun = api.loadCards()
// .then((res) => {
    
//     console.log(res.data)
//     return res.data.filter(item => item.type === "bun");
//   })

//   export let main = api.loadCards()
//   .then((res) => {
//       return res.data.filter(item => item.type === "main");
//     })


//     export let sauce = async () => await api.loadCards()
// .then((res) => {
//     return res.data.filter(item => item.type === "sauce");
//   })

// // async function loadSauce() {
   
// // let sauce = {};

// //     return await api.loadCards()
// // .then((res) => {
// //     return res.data.filter(item => item.type === "sauce");
// //   })

  

// }

// console.log(sauce)